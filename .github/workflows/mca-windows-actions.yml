name: mca-windows
on:
  push:
    paths:
      - 'mca/windows/**'
      - 'common/queued_network/**'

jobs:
  mca-windows-builds:
    runs-on: windows-latest
    steps:
      - uses: actions/checkout@v2
      - run: type mca/standard/main.cpp
      - run: type mca/windows/main.cpp
