name: mca-windows
on:
  push:
    paths:
      - 'mca/windows/**'
      - 'common/queued_network/**'

jobs:
  mca-windows-builds:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v2
      - run: cat mca/standard/main.cpp
      - run: cat mca/windows/main.cpp
